[package]
name = "bevy_infinite_world_test"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.1", features = ["debug"] }
bevy_asset_loader = { version = "0.27.0", features = ["2d"] }
